diff --git a/setup.py b/setup.py
index 1234567..abcdefg 100644
--- a/setup.py
+++ b/setup.py
@@ -13,6 +13,13 @@ from torch.utils.cpp_extension import (CUDA_HOME, BuildExtension, CppExtension,

 __version__ = '2.1.2'
 URL = 'https://github.com/rusty1s/pytorch_scatter'
+
+# Auto-generate local version from CUDA and PyTorch versions.
+cuda_version = torch.version.cuda
+torch_version = ".".join(torch.__version__.split(".")[:2])
+if cuda_version:
+    __version__ = f"{__version__}+cu.{cuda_version}.torch.{torch_version}"
+

 WITH_CUDA = False
 if torch.cuda.is_available():
